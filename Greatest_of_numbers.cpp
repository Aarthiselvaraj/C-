#include <iostream>
using namespace std;
int main()
{
 int a,b,c,num;
 num=a,b,c;
 cout<<"\n"<<"Enter the three numbers:";
   cin>>a>>b>>c;
 if(char str[]="num")
    cout<<"invalid input";
 else if(a>=b && a>=c)
    cout<<"the largest number is:"<<a;
 else if(b>=a && b>=c)
    cout<<"the largest number is:"<<b;
 else if(c>=a&&c>=b)
    cout<<"the largest number is:"<<c;
 else if(a==b && a==c)
    cout<<"enter the differnt numbers";
 else
    cout<<"invalid input";
}
